- Create a Photo component that will render thumbnail url only
- Render all photos using map and Photo component
- When we click on any photo. On the top of the screen, it will show that image in large (render it's `url`)
